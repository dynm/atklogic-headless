interface:
  display_name: "ATKLogic Headless"
  short_description: "Headless capture and extensible protocol decoding"
  default_prompt: "Use $atklogic-headless to capture logic signals, decode a supported protocol, or add and validate a requested protocol decoder."
